# Backend do Gerador de Propostas Comerciais
# Copie este arquivo para .env e preencha os valores

# Porta do servidor Express (padrão: 3333)
PORT=3333

# Google Places API Key (opcional — sem ela o sistema usa Nominatim/OSM como fallback)
# GOOGLE_PLACES_API_KEY=sua_chave_aqui
